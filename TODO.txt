
// LECTURE D'UN FICHIER AU FORMAT ARFF

@attribute dans la ligne =>
	String attribut = chaine vide
	Array valeur = tableau vide

	Pour chaque chaine de la ligne split =>
		Si c'est la premiere de la ligne, attribut = chaine
		Sinon, valeur.add(chaine)

	jeu.ajouterAttribut(attribut, valeurs);

@data dans la ligne =>
	Array exemple = tableau vide

	Pour chaque chaine de la ligne split =>
		exemple.add(chaine)
	
	jeu.ajouterExemple(exemple)

// TODO

vérifier que le clonage (méthode attributs de la classe JeuDonnees) fait bien son boulot
thread enlevés, synchronized ? avec les threads, l'affichage du modele se fait trop tot donc y'a rien...

// ARBRE DE DECISION

Si j'ai bien compris comment un arbre se construit :
- Création d'un premier noeud contenant le jeu de données complet
- Ce noeud choisit un attribut : calcul du gain d'information (semestre 1) ou du gain d'entropie (semestre 2) ?
- Pour chaque valeur de cet attribut, le noeud instancie un noeud fils contenant le jeu de données en ne gardant que les exemples concernés par la valeur
  Ex : l'attribut choisi est Prévision et a 3 valeurs : Soleil, Pluie, Couvert
  3 noeuds sont créés, ayant tous pour noeud père le même noeud
  1 noeud n'aura que les exemples où Prévision = Soleil
  1 noeud n'aura que les exemples où Prévision = Pluie
  1 noeud n'aura que les exemples où Prévision = Couvert
- Chaque noeud ainsi créé fera un choix d'un nouvel attribut, s'il n'a pas que des "+" ou des "OUI"

Récap' :
- Une instance d'arbre (classe Arbre) créé un noeud racine (classe Noeud) en lui passant le jeu complet
  L'arbre execute le noeud racine (racine.start())
- Chaque noeud choisit un attribut et créé des noeuds fils, ssi ce n'est pas une feuille pure

// ENTROPIE

Entropie semestre 1 : http://moodle.univ-angers.fr/pluginfile.php/579146/mod_resource/content/1/ArbreDecision.pdf Algo page 9 et calcul de gain page 13
Entropie: http://www-lisic.univ-littoral.fr/~verel/TEACHING/13-14/IASF-M1app/cm10.pdf
Entropie > 1 : http://people.revoledu.com/kardi/tutorial/DecisionTree/how-to-measure-impurity.htm
Post-élagage : https://fr.wikipedia.org/wiki/Arbre_de_d%C3%A9cision_(apprentissage)#Le_post-.C3.A9lagage

// SUJET DU PROJET

Tester votre programme sur des exemples assez conséquents que l'on peut trouver sur le site :
http://archive.ics.uci.edu/ml/












// POUBELLE

		// Supprime la colonne attribut des exemples
		/*Object[] attributs = this.attributs.keySet().toArray(); // converti la map en liste des clefs (récupération des attributs)
		int indice_attribut = Arrays.asList(attributs).indexOf(attribut);
		// Pour chaque exemple
		for (ArrayList<String> exemple : this.exemples) {
			// Supprime la valeur à la colonne de l'attribut
			exemple.remove(indice_attribut);
		}*/